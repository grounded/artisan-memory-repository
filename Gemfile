source 'http://rubygems.org'

gem 'rake', '0.9.2.2'
gem 'activemodel', '3.1.10'

# gem 'artisan-repository', '0.0.5'
gem 'artisan-repository', :path => File.expand_path('../artisan-repository', File.dirname(__FILE__))

group :test, :development do
  gem 'rspec', '2.11.0'
end
